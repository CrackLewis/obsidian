
核心思想：
- ngrok会提供一个临时公有域名用于外部连入，但这个域名每次会变
- 目标机器启动ngrok服务，建立与外部的连接
- 外部通过ngrok提供的公有域名与目标机器连接

基本步骤：
- 本地安装ngrok
- 设置auth token
- 通过`ngrok <协议类型，如http/tcp> <端口>`启动对外服务
	- 如果是ssh：`ngrok tcp 22`

临时域名是免费的；固定域名需要10usd的月租